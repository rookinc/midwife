# midwife.env — default pathing + runtime knobs
# Safe to edit. Works on Termux and macOS.

# --- Agent home (where agents live) ---
# Termux default: $HOME/xagents
XAGENTS_DIR="$HOME/xagents"

# --- Default agent root if user only supplies name ---
# Expanded as: ${XAGENTS_DIR}/{AGENT_NAME}
AGENT_ROOT_TEMPLATE="${XAGENTS_DIR}/{AGENT_NAME}"

# --- Midwife python entrypoint (this file) ---
MIDWIFE_PY="$HOME/xagents/midwife/midwife.py"

# --- Default venv activation (wizard can use this if present) ---
DEFAULT_VENV_ACTIVATE="$HOME/xagents/.venv/bin/activate"
PYTHON_BIN="$HOME/xagents/.venv/bin/python"

# --- Python executable to use ---
PYTHON_BIN="python"

# --- Location defaults (until you add real location providers) ---
DEFAULT_LOCATION_DISPLAY="unknown"
DEFAULT_LOCATION_PRECISION="city"
DEFAULT_LOCATION_CONFIDENCE="0.3"

# --- Registry paths (local registry artifacts) ---
REGISTRY_DIR_NAME="registry"
REGISTRY_RESPONSES_DIR="responses"

# --- Policy ---
TRANSPARENCY_MODE="public_by_default"

# --- Platform hints (override on macOS if desired) ---
HOST_PLATFORM="termux"
HOST_DEVICE="android"
